We built an AI that:
takes a movie title
takes a user-selected mood
uses the movie’s plot summary
generates lyrics inspired by the movie plot and mood
runs those generated lyrics through a trained sentiment classifier
checks whether the generated lyrics match the intended mood
